[project]
name = "examen"
version = "0.1.0"
description = ""
authors = [
    {name = "Federico Da Silva",email = "fededamel@fiuna.edu.py"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi (>=0.141.1,<0.142.0)",
    "uvicorn (>=0.53.0,<0.54.0)",
    "sqlalchemy (>=2.0.54,<3.0.0)",
    "pydantic (>=2.13.5,<3.0.0)"
]

[tool.poetry]
packages = [{include = "examen", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
