[project]
name = "nasa"
version = "0.1.0"
description = ""
authors = [
    {name = "Federico",email = "fededamel@fiuna.edu.py"}
]
requires-python = ">=3.12"
dependencies = [
    "numpy (>=2.5.2,<3.0.0)",
    "matplotlib (>=3.11.1,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
